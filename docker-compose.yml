version: '3.8'

services:
  frontend:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html:ro
    restart: unless-stopped
    networks:
      - default
      - echo-shared

networks:
  echo-shared:
    external: true
